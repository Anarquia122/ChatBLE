import { StyleSheet } from "react-native";

const graphsStyles = StyleSheet.create({
    title: {
        fontSize: 22, 
        fontWeight: 'bold', 
        textAlign: 'center', 
        marginTop: 10, 
        color: '#ddd',
    },
    lineChart: {
        marginVertical: 24, 
        borderRadius: 16,
    },
});

export default graphsStyles;